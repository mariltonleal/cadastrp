export interface Cliente {
  id: string;
  nome: string;
  email: string;
  telefone: string;
  endereco: string;
  userId: string;
  createdAt: string;
}

export interface User {
  id: string;
  email: string;
}